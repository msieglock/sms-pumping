{
  "name": "@smsguard/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply smsguard-db",
    "db:migrate:local": "wrangler d1 migrations apply smsguard-db --local",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.22.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "jose": "^5.2.0",
    "stripe": "^14.11.0",
    "@sendgrid/mail": "^8.1.0",
    "@anthropic-ai/sdk": "^0.10.0"
  }
}
